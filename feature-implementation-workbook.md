# 기능 구현 TODO/빈칸 워크북

이 문서는 `01-html-css`부터 `17-interview-prep`, 그리고 `monorepo-mini-app`까지 모든 기능 구현을 공부용 TODO/빈칸 형태로 다시 점검하기 위한 워크북입니다.

목표는 정답을 빨리 보는 것이 아니라, 각 기능을 "왜 필요한가", "어느 파일에서 구현되는가", "어떻게 검증하는가", "면접에서 어떻게 설명하는가"까지 자기 손으로 채우는 것입니다.

## 사용 방법

1. 각 단계의 `README.md`를 읽고 이 워크북의 빈칸을 먼저 채웁니다.
2. `problems.md`와 실제 코드의 `TODO`, `____`, `빈칸`을 직접 해결합니다.
3. 실행 결과를 확인한 뒤 검증 명령 또는 캡처 근거를 적습니다.
4. 막히면 `answers.md`를 보되, 답을 본 이유와 다시 설명할 문장을 남깁니다.
5. 마지막에는 각 단계마다 "내가 구현한 기능 한 문장"을 작성합니다.

완벽하게 정리됐는지 판단하는 기준은 네 가지입니다.

- 개념. 이 기능이 왜 필요한지 설명할 수 있다.
- 구현. 어느 파일의 어느 흐름을 고쳐야 하는지 안다.
- 검증. 브라우저, API 요청, 테스트, 빌드 중 하나로 확인했다.
- 설명. 면접에서 자기 코드 기준으로 1분 안에 말할 수 있다.

## 이 워크북을 읽는 법 (범례)

각 단계는 같은 순서로 구성됩니다. 위에서 아래로 내려가면 "개념 → 구현 → 함정 → 면접 → 검증" 흐름이 됩니다.

- **머리말 줄** `> 난이도 ★★☆ · 예상 시간 60분 · 선행 OO` — 체감 난이도와 한 바퀴 도는 데 걸리는 대략 시간, 먼저 해두면 좋은 트랙.
  - 난이도 `★☆☆` 쉬움 / `★★☆` 보통 / `★★★` 어렵거나 개념이 많음.
- **개념 빈칸** — 정의·용어를 스스로 채우는 칸. 막히면 바로 아래 `└ 힌트:` 줄을 봅니다.
  - `└ 힌트:` 줄은 **정답 단어를 알려주지 않습니다.** "어느 파일·어느 함수를 보고, 어떤 개념 범주인지"만 가리킵니다. 정답은 직접 적습니다.
- **구현 TODO** — 실제 코드에서 채울 작업. 큰 작업은 `└` 하위 단계로 잘게 쪼개 한 줄씩 따라갈 수 있게 했습니다.
- **흔한 실수·함정** — 신입이 자주 빠지는 자리. 여기서 막혀봤다면 면접에서 좋은 이야깃거리가 됩니다.
- **면접 꼬리질문** — 이 단계를 "했다"고 말하면 면접관이 이어서 물을 법한 질문. 소리 내어 1분 안에 답해봅니다.
- **자가 점검** — 정답지 없이 스스로 답해보는 질문. 막히면 그 항목이 약점입니다.
- **검증 빈칸 / 설명 문장** — 무엇으로 확인했는지, 면접에서 한 문장으로 어떻게 말할지.

> 규칙: `____`와 `빈칸`은 **직접** 채웁니다. 힌트·꼬리질문·자가 점검은 정답을 적은 칸이 아니라 *생각을 끌어내는 장치*입니다. 답을 `answers.md`에서 봤다면, 본 이유와 "다시 내 말로" 한 문장을 꼭 남기세요.

## 01 HTML/CSS 자기소개 페이지

관련 파일: `01-html-css/starter/index.html`, `01-html-css/starter/styles.css`

> 난이도 ★☆☆ · 예상 시간 40분 · 선행 없음 (시작 트랙)

### 개념 빈칸

- HTML에서 페이지의 의미 구조를 만드는 태그를 ____ 태그라고 한다.
  - └ 힌트: `<div>`처럼 의미 없는 상자가 아니라 `<header>`, `<nav>`, `<section>`처럼 "이름만 봐도 역할을 아는" 태그를 묶어 부르는 말. index.html의 바깥 골격 태그들을 보세요.
- 한 페이지의 핵심 제목은 보통 `<____>` 태그로 작성한다.
  - └ 힌트: 제목 태그 h1~h6 중 페이지당 보통 하나만 쓰는 최상위 단계.
- 이미지에는 접근성을 위해 `____` 속성을 반드시 작성한다.
  - └ 힌트: 이미지가 안 보이거나 스크린리더가 읽을 때 대신 나오는 텍스트 속성. `<img src=... ____=...>`.
- Flexbox는 한 방향 배치에 강하고, Grid는 ____ 배치에 강하다.
  - └ 힌트: 행과 열을 동시에 다루는 차원 수를 생각하세요.
- 모바일 화면에서 가로 배치를 세로로 바꾸려면 media query 안에서 `flex-direction: ____`을 사용한다.
  - └ 힌트: 기본값은 `row`(가로). 세로로 쌓는 값은? styles.css의 `@media` 블록을 보세요.

### 구현 TODO

- [ ] `<title>`과 `<h1>`에 자기 이름을 넣는다.
- [ ] 관심 분야와 성장 목표 문장을 자기 말로 채운다.
- [ ] 프로필 이미지 `src`와 `alt`를 의미 있게 채운다.
- [ ] 연락 링크의 `mailto:` 주소를 채운다.
- [ ] CSS를 단계별로 채운다.
  - └ `body`의 `font-family`를 지정한다.
  - └ 자기소개 영역을 `display: flex`로 배치한다.
  - └ 카드 목록을 `display: grid`로 배치한다.
  - └ 버튼/링크 `:hover` 배경색을 지정한다.
- [ ] 모바일 media query에서 세로 배치(`flex-direction`)를 적용한다.

### 흔한 실수·함정

- `alt`를 빈 문자열이나 "이미지"처럼 무의미하게 채운다 → 접근성 점수도, 면접 인상도 떨어진다.
- 의미 태그 대신 `<div>`만 남발한다 → "왜 `<section>`을 안 썼나요?" 질문에 답이 막힌다.
- Flex/Grid를 둘 다 `display: flex`로 통일해 버려서 "차이를 모른다"는 인상을 준다.
- 모바일 확인을 안 하고 데스크톱만 보고 끝낸다.

### 면접 꼬리질문

- "시맨틱 태그를 쓰면 `<div>`보다 뭐가 좋나요?" (접근성·SEO·가독성 중 하나는 말할 수 있어야)
- "Flexbox와 Grid를 각각 언제 쓰나요?"
- "반응형은 어떻게 구현했나요? `rem`/`em`/`%`/미디어쿼리 중 무엇을 썼나요?"

### 자가 점검

- 이 페이지에서 `<div>`를 의미 태그로 바꿀 수 있는 자리가 어디인지 짚을 수 있는가?
- 브라우저 개발자도구로 모바일 너비를 만들어 깨지는 곳을 직접 봤는가?

### 검증 빈칸

- 브라우저에서 연 파일 경로: ____
- 모바일 너비에서 깨지지 않는지 확인한 방법: ____
- 내가 설명할 CSS 속성 2개: ____, ____

### 설명 문장

이 페이지는 ____ 태그로 구조를 나누고, ____와 ____를 사용해 자기소개 영역과 카드 목록을 반응형으로 배치했습니다.

## 02 JavaScript 이벤트와 계산기

관련 파일: `02-javascript-basics/starter/app.js`

> 난이도 ★★☆ · 예상 시간 60분 · 선행 01

### 개념 빈칸

- 브라우저의 HTML 요소를 JavaScript에서 다루는 객체 모델을 ____이라고 한다.
  - └ 힌트: Document Object Model의 약자. `document.querySelector(...)`로 만지는 그 트리.
- 버튼 클릭처럼 사용자의 행동이 발생하는 것을 ____라고 한다.
  - └ 힌트: `addEventListener("click", ...)`에서 "click"이 그 한 종류.
- input의 현재 값은 보통 `element.____`로 읽는다.
  - └ 힌트: 텍스트 입력칸이 들고 있는 글자를 담는 속성 이름.
- 배열 끝에 값을 추가할 때 사용하는 메서드는 `____`이다.
  - └ 힌트: 배열 앞이 아니라 *끝*에 밀어넣는 메서드.
- 화면을 다시 그리는 함수를 따로 두면 데이터 변경과 ____를 분리할 수 있다.
  - └ 힌트: "데이터를 바꾸는 일"과 "그 데이터를 화면에 그리는 일"을 나눈다는 뜻. 후자를 가리키는 단어.

### 구현 TODO

- [ ] 이름 입력값을 읽어 환영 문구를 바꾼다.
- [ ] `calculate` 함수를 완성한다.
  - └ 두 입력값을 숫자로 변환한다.
  - └ 더하기·빼기·곱하기·나누기 분기를 작성한다.
  - └ 0으로 나누는 경우 오류 메시지를 반환한다.
- [ ] 할 일 목록 기능을 완성한다.
  - └ 입력한 할 일을 배열에 넣는다.
  - └ 목록을 렌더링할 때 `<li>` 요소를 만든다.
  - └ 데이터 변경 후 렌더 함수를 다시 호출한다.

### 흔한 실수·함정

- input 값은 항상 문자열이라 `"3" + "4"`가 `"34"`가 된다 → 숫자 변환을 빼먹으면 계산이 깨진다.
- 0으로 나누기를 막지 않아 `Infinity`나 `NaN`이 화면에 나온다.
- 배열만 바꾸고 렌더 함수를 안 불러서 화면이 안 바뀐다(또는 반대).
- `addEventListener`를 요소가 그려지기 전에 걸어 `null`에 이벤트를 붙이려다 에러.

### 면접 꼬리질문

- "이벤트 리스너는 어디에 붙였나요? 왜 거기였나요?"
- "`var`/`let`/`const`를 어떻게 구분해서 썼나요?"
- "데이터와 렌더를 왜 분리했나요? 안 나누면 어떤 문제가 생기나요?"

### 자가 점검

- 버튼을 눌렀을 때 코드가 실행되는 순서를 말로 설명할 수 있는가? (클릭 → 함수 → 값 읽기 → 계산 → 화면)
- `NaN`이 왜 나오는지, 어떻게 막는지 아는가?

### 검증 빈칸

- 클릭 이벤트가 연결된 버튼 id: ____
- 계산기에서 테스트한 입력값: ____ + ____ = ____
- 할 일 추가 후 배열과 화면이 함께 바뀐 이유: ____

### 설명 문장

사용자가 버튼을 누르면 ____가 실행되고, 함수가 input 값을 읽은 뒤 배열이나 DOM을 변경해 화면을 갱신합니다.

## 03 React Todo

관련 파일: `03-react-todo/src/App.jsx`, `03-react-todo/src/components/TodoItem.jsx`

> 난이도 ★★☆ · 예상 시간 70분 · 선행 02

### 개념 빈칸

- React에서 화면에 영향을 주는 값은 보통 ____로 관리한다.
  - └ 힌트: `const [x, setX] = useState(...)`로 만드는 그것. 바뀌면 React가 다시 그린다.
- 부모 컴포넌트가 자식 컴포넌트에 넘기는 값은 ____라고 한다.
  - └ 힌트: `<TodoItem todo={...} onToggle={...} />`에서 자식이 받는 입력들.
- 배열 state를 수정할 때 기존 배열을 직접 바꾸지 않고 새 배열을 만드는 이유는 ____를 지키기 위해서다.
  - └ 힌트: 원본을 안 건드리고 새 값을 만드는 성질. React가 변경을 감지하는 근거가 된다.
- 입력창의 변경 이벤트에서 실제 입력값은 `event.target.____`에 있다.
  - └ 힌트: 02단계 input과 같은 속성.
- 리스트 렌더링에서 `key`가 필요한 이유는 React가 각 항목의 ____를 추적하기 위해서다.
  - └ 힌트: 항목이 추가/삭제/재정렬될 때 "누가 누구인지" 구분하는 표식.

### 구현 TODO

- [ ] `remainingCount`를 `todos.filter`로 계산한다.
- [ ] 새 Todo 추가를 완성한다.
  - └ 제목을 `input.trim()`으로 만든다.
  - └ 빈 문자열은 추가하지 않도록 막는다.
  - └ 기존 배열을 펼친 새 배열로 state를 갱신한다.
- [ ] `map`으로 해당 id의 `done` 값을 반대로 바꾼다.
- [ ] input `onChange`에서 `setInput(event.target.value)`를 호출한다.
- [ ] `TodoItem` 버튼 클릭 시 부모의 `onToggle`을 실행한다.

### 흔한 실수·함정

- `todos.push(...)`로 원본 배열을 직접 바꾼다 → 화면이 안 갱신된다(불변성 위반).
- `key`에 배열 index를 쓴다 → 항목 순서가 바뀌면 잘못 렌더된다. id를 쓰자.
- `trim()`을 빼서 공백만 있는 할 일이 추가된다.
- 토글에서 모든 항목의 done을 바꿔버린다(특정 id만 바꿔야 함).

### 면접 꼬리질문

- "state와 props의 차이가 뭔가요?"
- "왜 배열을 직접 수정하면 안 되나요? 불변성이 뭔가요?"
- "리스트 렌더링에서 key는 왜 필요한가요? index를 쓰면 왜 위험한가요?"
- "리렌더링은 언제 일어나나요?"

### 자가 점검

- `setTodos`를 호출하면 그 다음에 무슨 일이 순서대로 일어나는지 설명할 수 있는가?
- 같은 화면을 02단계 바닐라 JS로 만든 것과 비교해 React가 뭘 대신 해주는지 말할 수 있는가?

### 검증 빈칸

- 실행 명령: ____
- 빌드 명령: ____
- 완료 토글 시 바뀌는 state 필드: ____

### 설명 문장

Todo 앱은 ____를 단일 기준으로 삼고, 입력 이벤트와 토글 이벤트가 state를 바꾸면 React가 화면을 다시 렌더링합니다.

## 04 Node 게시판 REST API

관련 파일: `04-node-board-api/src/server.js`, `04-node-board-api/requests.http`

> 난이도 ★★☆ · 예상 시간 60분 · 선행 02

### 개념 빈칸

- REST에서 리소스 목록 조회는 보통 HTTP 메서드 ____를 사용한다.
  - └ 힌트: 읽기 전용·안전한 메서드.
- 리소스 생성 성공 상태 코드는 ____이다.
  - └ 힌트: 2xx 중 "새로 만들어짐"을 뜻하는 코드. server.js의 `res.status(...)` 호출부 참고.
- 리소스 삭제 성공 후 응답 본문이 없으면 상태 코드 ____를 자주 사용한다.
  - └ 힌트: 2xx 중 "성공했고 보낼 본문 없음".
- URL의 `:id` 값은 `req.____.id`에서 읽는다.
  - └ 힌트: 쿼리스트링(`req.query`)이 아니라 경로 파라미터를 담는 객체.
- JSON 요청 본문을 읽기 위해 Express에 `express.____()` 미들웨어를 등록한다.
  - └ 힌트: 이걸 안 붙이면 `req.body`가 `undefined`가 된다. 본문 형식 이름과 같다.

### 구현 TODO

- [ ] `GET /posts`가 게시글 배열을 응답하게 한다.
- [ ] `GET /posts/:id`를 완성한다.
  - └ `req.params.id`를 꺼낸다.
  - └ `Number(...)`로 숫자 변환한다.
  - └ `find`로 일치하는 글을 찾는다.
  - └ 없으면 적절한 상태 코드로 응답한다.
- [ ] `POST /posts`에서 title과 content를 body에서 읽는다.
- [ ] `PUT /posts/:id`에서 기존 값과 요청 값을 병합한다.
- [ ] `DELETE /posts/:id`에서 배열에서 항목을 제거한다.

### 흔한 실수·함정

- `:id`를 숫자로 변환하지 않아 `"1" === 1`이 `false`가 되어 404가 난다.
- `express.json()`을 안 붙여 `req.body`가 `undefined`다.
- 생성/삭제/조회의 상태 코드를 전부 200으로 통일해 버린다.
- PUT에서 요청에 없는 필드를 `undefined`로 덮어써 기존 데이터를 날린다.

### 면접 꼬리질문

- "PUT과 PATCH의 차이는 뭔가요?"
- "201과 200, 204를 각각 언제 쓰나요?"
- "REST에서 멱등성(idempotent)이 뭔가요? GET/PUT/DELETE는 멱등인가요?"
- "이 API를 실제 DB로 바꾸면 뭐가 달라지나요?" (05단계로 연결)

### 자가 점검

- 6개 엔드포인트 각각의 메서드·경로·성공 상태 코드를 표로 말할 수 있는가?
- `requests.http`로 직접 6개를 다 호출해 응답 코드를 눈으로 확인했는가?

### 검증 빈칸

- health check URL: ____
- 생성 요청의 상태 코드: ____
- 없는 id 조회 시 상태 코드: ____

### 설명 문장

게시판 API는 ____ 메서드로 조회, 생성, 수정, 삭제를 나누고, 성공과 실패를 HTTP 상태 코드로 구분했습니다.

## 05 MongoDB 게시판 API

관련 파일: `05-database-mongodb/src/db.js`, `05-database-mongodb/src/models/Post.js`, `05-database-mongodb/src/server.js`

> 난이도 ★★☆ · 예상 시간 70분 · 선행 04

### 개념 빈칸

- MongoDB와 Node.js를 연결할 때 이 프로젝트에서 사용하는 ODM은 ____이다.
  - └ 힌트: `package.json` 의존성과 `db.js`의 `require/import`를 보세요. 스키마·모델을 제공하는 라이브러리.
- Mongoose에서 컬렉션 문서의 모양을 정의하는 객체는 ____이다.
  - └ 힌트: `Post.js`에서 `new ____({...})`로 필드 타입을 선언하는 것.
- 데이터가 서버 재시작 후에도 남는 성질을 ____이라고 한다.
  - └ 힌트: 04단계 배열은 재시작하면 사라졌다. DB는 이 성질이 있다.
- `findByIdAndUpdate`에서 수정된 최신 문서를 받으려면 옵션 `____: true`를 사용한다.
  - └ 힌트: 기본값은 수정 *전* 문서를 돌려준다. "갱신된 새 문서"를 달라는 옵션 이름.
- 비동기 라우터에서 에러를 중앙 처리로 넘길 때 `____(error)`를 호출한다.
  - └ 힌트: Express 핸들러의 세 번째 인자. 호출하면 에러 미들웨어로 넘어간다.

### 구현 TODO

- [ ] author 기본값을 스키마 default로 채운다.
- [ ] 생성 API에서 title, content, author를 body에서 읽는다.
- [ ] 수정 API에서 title, content, author 값을 body에서 읽는다.
- [ ] 삭제 API에서 `req.params.id`로 문서를 삭제한다.
- [ ] DB 연결 실패 또는 ObjectId 오류가 응답으로 드러나는지 확인한다.

### 흔한 실수·함정

- `await`를 빼먹어 Promise 객체가 그대로 응답된다.
- 잘못된 형식의 id를 주면 `CastError`가 나는데, 이걸 500이 아니라 400/404로 다루는 처리를 빼먹는다.
- `findByIdAndUpdate`에서 `new: true`를 안 줘서 수정 전 데이터가 응답된다.
- 환경변수(연결 문자열)를 코드에 하드코딩한다.

### 면접 꼬리질문

- "04단계의 배열 저장과 MongoDB 저장의 차이는 뭔가요?" (영속성)
- "스키마/모델/도큐먼트가 각각 뭔가요?"
- "관계형 DB(10단계)와 MongoDB의 차이를 한 가지만 말해보세요."
- "비동기 라우터에서 에러는 어떻게 처리했나요?"

### 자가 점검

- 생성 → 서버 재시작 → 다시 조회했을 때 데이터가 남아있는 걸 직접 확인했는가?
- 04단계 코드와 비교해 "어디가 바뀌었는지"를 한 문장으로 말할 수 있는가?

### 검증 빈칸

- MongoDB 연결 문자열 환경변수 이름: ____
- 생성 후 다시 조회했을 때 같은 데이터가 보인 이유: ____
- Mongoose 모델 이름: ____

### 설명 문장

MongoDB 단계에서는 Express 라우터가 직접 배열을 고치는 대신, ____ 모델을 통해 데이터를 저장하고 조회하도록 바꿨습니다.

## 06 로그인과 JWT 인증

관련 파일: `06-login-auth/src/server.js`, `06-login-auth/src/auth.js`, `06-login-auth/src/users.js`

> 난이도 ★★★ · 예상 시간 80분 · 선행 04

### 개념 빈칸

- 비밀번호는 평문 저장 대신 ____로 저장해야 한다.
  - └ 힌트: 원문으로 되돌릴 수 없는 단방향 변환 결과. `auth.js`의 bcrypt 호출 결과물.
- bcrypt의 `hash(password, 10)`에서 10은 work factor, 즉 ____ 비용을 의미한다.
  - └ 힌트: 숫자가 커질수록 해시 1번에 드는 시간이 늘어난다. 무차별 대입을 느리게 만드는 것.
- 로그인 성공 후 서버가 발급하는 서명된 토큰은 ____이다.
  - └ 힌트: 세 부분이 점(`.`)으로 나뉜 그 토큰. `auth.js`의 sign 함수가 만든다.
- 보호 API는 요청 헤더 `____: Bearer <token>`을 확인한다.
  - └ 힌트: 인증 정보를 싣는 표준 헤더 이름.
- 인증 실패 상태 코드는 보통 ____이다.
  - └ 힌트: 4xx 중 "너 누군지 확인 안 됨". (권한 없음 403과 헷갈리지 말 것)

### 구현 TODO

- [ ] 회원가입을 완성한다.
  - └ `bcrypt.hash(password, 10)`으로 비밀번호를 해시한다.
  - └ 이름이 없을 때 사용할 기본 이름을 정한다.
  - └ 중복 이메일을 확인한다.
- [ ] 로그인을 완성한다.
  - └ `bcrypt.compare(password, user.passwordHash)`로 검증한다.
  - └ JWT payload에 user id와 email을 담는다.
- [ ] `/me` 라우트가 `requireAuth`를 통과한 사용자만 응답하게 한다.

### 흔한 실수·함정

- 응답에 `passwordHash`를 그대로 담아 비밀번호 해시가 노출된다.
- 비밀번호 검증을 `===`로 평문 비교한다(해시는 `compare`로 비교해야 함).
- JWT 비밀키를 코드에 하드코딩하거나 커밋한다.
- 401(인증 안 됨)과 403(권한 없음)을 구분 못 한다.
- "로그인 됨"을 프론트 state로만 믿고 서버에서 토큰 검증을 안 한다.

### 면접 꼬리질문

- "비밀번호를 왜 해시로 저장하나요? 암호화와 해시의 차이는?"
- "JWT는 어디에 저장하나요? 쿠키 vs localStorage 차이와 위험은?" (16단계 연결)
- "세션 방식과 JWT 방식의 차이는 뭔가요?"
- "토큰이 탈취되면 어떻게 되나요? 만료(exp)는 왜 두나요?"
- "401과 403의 차이는?"

### 자가 점검

- 회원가입 → 로그인 → 토큰으로 `/me` 호출까지의 흐름을 그림으로 그릴 수 있는가?
- 토큰 없이, 잘못된 토큰으로 `/me`를 호출했을 때 각각 어떤 코드가 나오는지 확인했는가?

### 검증 빈칸

- 회원가입 성공 상태 코드: ____
- 중복 이메일 상태 코드: ____
- 토큰 없이 `/me` 호출 시 상태 코드: ____

### 설명 문장

회원가입 때 비밀번호를 ____로 해시하고, 로그인 성공 시 ____를 발급해 이후 보호 API에서 요청자를 확인했습니다.

## 07 프론트와 백 연결 및 배포 준비

관련 파일: `07-project-deploy/src/server.js`, `07-project-deploy/public/app.js`, `07-project-deploy/deploy-checklist.md`

> 난이도 ★★☆ · 예상 시간 60분 · 선행 04, 06

### 개념 빈칸

- Express에서 정적 파일을 제공하는 미들웨어는 `express.____`이다.
  - └ 힌트: `public/` 폴더의 HTML/CSS/JS를 그대로 내보내는 미들웨어 이름.
- 같은 서버에서 API를 호출할 때 `API_BASE`는 보통 ____ 문자열로 둔다.
  - └ 힌트: 도메인을 안 적고 경로만 쓰면 같은 출처로 간다. `app.js`의 `API_BASE` 값을 보세요.
- 브라우저에서 HTTP 요청을 보낼 때 이 프로젝트가 사용하는 함수는 ____이다.
  - └ 힌트: 최신 브라우저 내장. `await ____("/api/...")`.
- 로컬과 배포 환경에서 달라지는 값은 ____ 변수로 분리한다.
  - └ 힌트: `.env`에 담고 `process.env`로 읽는 그것.
- 배포 전 실행 방법, 환경 변수, 검증 방법은 ____에 남겨야 한다.
  - └ 힌트: 이 트랙의 관련 파일 이름 그대로.

### 구현 TODO

- [ ] `API_BASE`를 같은 서버 기준으로 설정한다.
- [ ] 게시글 목록 렌더링에서 title, author, createdAt을 표시한다.
- [ ] 폼 입력값을 `trim()`해서 요청 body에 담는다.
- [ ] 서버 생성 API에서 title, content, author를 body에서 읽는다.
- [ ] 배포 체크리스트에 빌드, 환경 변수, 실행 확인을 기록한다.

### 흔한 실수·함정

- `.env`, `node_modules`를 커밋해 비밀값과 용량이 새어 나간다.
- 프론트에서 API 주소를 절대경로(`http://localhost:3000`)로 하드코딩해 배포 후 깨진다.
- CORS를 고려 안 해 다른 출처에서 호출 시 막힌다.
- 빌드 산출물(`dist`)을 깜빡하고 안 올리거나, 반대로 소스에 커밋한다.

### 면접 꼬리질문

- "프론트와 백을 같은 서버에서 서빙하면 CORS가 왜 필요 없어지나요?"
- "환경변수는 왜 코드에 안 넣고 분리하나요?"
- "배포 전 체크리스트에 뭘 넣었나요? 왜 그 항목인가요?"

### 자가 점검

- 정적 페이지 → API 호출 → 응답 렌더의 전체 흐름을 말할 수 있는가?
- 이 프로젝트를 처음 받은 사람이 README만 보고 실행할 수 있는가?

### 검증 빈칸

- API 목록 URL: ____
- 정적 페이지 URL: ____
- 배포 전 꼭 제외할 폴더: ____

### 설명 문장

7단계는 브라우저의 ____ 요청과 Express API를 연결하고, 배포 환경에서 필요한 설정을 문서화하는 단계입니다.

## 08 Career Hub Fullstack Portfolio

관련 파일: `08-fullstack-portfolio-project/src/App.jsx`, `08-fullstack-portfolio-project/server/index.js`, `08-fullstack-portfolio-project/server/auth.js`, `08-fullstack-portfolio-project/server/data-store.js`, `08-fullstack-portfolio-project/server/validators.js`

> 난이도 ★★★ · 예상 시간 3~4시간(여러 번에 나눠) · 선행 03, 04, 06, 07 · **제출용 대표 프로젝트**

이 단계는 면접에서 가장 길게 말하게 되는 프로젝트입니다. 빈칸을 넘어 "왜 이렇게 설계했는지"를 설명할 수 있어야 합니다.

### 전체 기능 지도 빈칸

- 앱 이름: ____
- 해결하는 문제: SI/SW 취업 준비자가 ____와 ____를 관리한다.
  - └ 힌트: 화면 탭 이름(지원 현황 / 프로젝트)을 보세요.
- 프론트엔드 스택: React + ____
  - └ 힌트: 번들러/개발서버. `vite.config.js`가 있다.
- 백엔드 스택: Node.js + ____
  - └ 힌트: 04~07단계에서 계속 쓴 그 웹 프레임워크.
- 인증 방식: bcrypt + ____
  - └ 힌트: 06단계에서 발급한 그 토큰.
- 저장 방식: JSON 파일 저장소, 추후 ____ 또는 PostgreSQL로 교체 가능
  - └ 힌트: 05단계에서 쓴 문서형 DB.
- 핵심 검증 명령: `npm run ____`
  - └ 힌트: lint·test·build·audit을 한 번에 도는 통합 명령. `package.json` scripts를 보세요.

### 프론트엔드 구현 TODO

- [ ] 로그인/회원가입 화면을 완성한다.
  - └ `mode` state로 로그인/회원가입 화면을 전환한다.
  - └ 로그인 성공 후 token과 user를 state에 저장한다.
  - └ token은 새로고침 후 유지되도록 `____`에 저장한다. (힌트: 브라우저에 키-값으로 남기는 저장소. 보안 트레이드오프는 16단계와 연결)
- [ ] `loadAll`에서 `/api/me`, `/api/dashboard`, `/api/applications`, `/api/projects`를 한 번에 갱신한다.
- [ ] 지원 현황 폼에서 생성과 수정 모드를 `editingId`로 구분한다.
- [ ] 프로젝트 폼에서 기술 스택 문자열을 배열로 변환한다.
- [ ] 탭 state로 지원 현황, 프로젝트, 학습 연결 화면을 전환한다.
- [ ] ErrorBoundary로 렌더링 예외 시 대체 화면을 보여준다.

### 백엔드 API 구현 TODO

- [ ] `/api/health`로 서버 상태를 반환한다.
- [ ] `/api/openapi.json`으로 API 문서를 노출한다.
- [ ] `/api/auth/register`를 완성한다.
  - └ 입력 검증(validators)을 통과시킨다.
  - └ 중복 이메일을 확인한다.
  - └ 비밀번호를 해시한다.
- [ ] `/api/auth/login`에서 비밀번호 검증 후 JWT를 발급한다.
- [ ] `/api/me`에서 현재 사용자 공개 정보를 반환한다.
- [ ] `/api/dashboard`에서 지원 현황과 프로젝트 기반 지표를 계산한다.
- [ ] `/api/applications` CRUD를 인증 사용자 기준으로 처리한다.
- [ ] `/api/projects` CRUD를 인증 사용자 기준으로 처리한다.
- [ ] API 오류를 의미 있게 구분한다.
  - └ 400 입력 오류 / 401 인증 실패 / 404 없음 / 409 충돌(중복) / 500 서버 오류.

### 저장소 계층 빈칸

- 저장소 파일: `server/____.js`
  - └ 힌트: 관련 파일 목록에 이미 있다. "데이터를 읽고 쓰는" 계층.
- 사용자에게 노출하면 안 되는 필드: ____
  - └ 힌트: 06단계에서도 새면 안 됐던 그 필드.
- 이메일 비교 전 적용하는 정규화: ____
  - └ 힌트: `User@Email.com`과 `user@email.com`을 같게 보려면? 대소문자 관련.
- 저장소 계층을 분리한 이유: 라우터가 특정 ____ 방식에 묶이지 않게 하기 위해서다.
  - └ 힌트: 지금은 JSON 파일, 나중엔 DB로 바꿀 그 "방식".

### 검증과 품질 TODO

- [ ] `npm run lint`로 ESLint를 통과한다.
- [ ] `npm run format:check`로 Prettier 포맷을 확인한다.
- [ ] `npm run test:unit`으로 서버, 저장소, 검증, 프론트 테스트를 실행한다.
- [ ] `npm run test:api`로 실제 서버 기준 인증과 CRUD smoke test를 실행한다.
- [ ] `npm run build`로 Vite production build를 만든다.
- [ ] `npm run audit:submit`으로 제출 필수 파일과 보안 항목을 확인한다.
- [ ] `npm run verify`로 위 항목을 한 번에 실행한다.

### 보안/운영 빈칸

- 기본 보안 헤더는 ____ 미들웨어로 설정한다.
  - └ 힌트: Express용 보안 헤더 패키지. `server/index.js`의 `app.use(...)`를 보세요.
- 로그인과 API 요청 남용은 ____ 미들웨어로 제한한다.
  - └ 힌트: 일정 시간당 요청 수를 제한하는 미들웨어(rate limit 계열).
- request body 크기 제한은 `express.json({ limit: "____" })`처럼 설정한다.
  - └ 힌트: 너무 큰 본문(예: `1mb`)을 막는 값. 실제 설정값을 코드에서 확인.
- 구조적 JSON 로그는 ____와 pino-http로 남긴다.
  - └ 힌트: pino 계열 로거. `server/logger.js` 참고.
- 요청 추적용 헤더 이름은 `____`이다.
  - └ 힌트: 요청마다 고유 id를 싣는 헤더. `x-`로 시작.
- 컨테이너 종료 신호 SIGTERM/SIGINT에서는 ____ 셧다운을 수행한다.
  - └ 힌트: 진행 중 요청을 끊지 않고 정리 후 닫는 방식(우아한…).

### 흔한 실수·함정

- 응답 객체에 `passwordHash`가 섞여 나간다(저장소 계층에서 걸러야 함).
- 다른 사용자의 application/project를 조회·수정할 수 있다(소유자 검사 누락 = 권한 우회).
- JWT 비밀키·`.env`를 커밋한다.
- 검증(validators)을 프론트에만 두고 서버에서 안 한다(서버 검증이 진짜 방어선).
- 에러를 전부 500으로 던져 400/404/409 구분이 없다.

### 면접 꼬리질문 (이 프로젝트는 깊게 들어옵니다)

- "왜 JSON 파일 저장소를 썼나요? DB로 바꾸려면 어디를 고치나요?" (저장소 계층 분리의 가치)
- "인증은 어떻게 구현했나요? 토큰은 어디 저장하고 그 위험은?"
- "한 사용자가 다른 사용자 데이터를 못 보게 어떻게 막았나요?"
- "입력 검증은 어디서 하나요? 프론트와 백 둘 다 한다면 왜죠?"
- "helmet/rate limit은 각각 무슨 공격을 막나요?"
- "OpenAPI 문서는 왜 만들었나요? 코드와 어떻게 동기화하나요?"
- "테스트는 어떤 층위로 짰나요? unit과 api 테스트의 차이는?"
- "이 프로젝트에서 가장 어려웠던 버그와 해결 과정을 말해보세요." (STAR로 준비)

### 자가 점검

- 회원가입부터 대시보드 지표 계산까지 데이터가 흐르는 경로를 화이트보드에 그릴 수 있는가?
- `npm run verify`가 통과하는가? 실패하면 어느 단계에서 깨지는지 읽을 수 있는가?
- 라우터 → 검증 → 저장소 → 응답의 책임 분리를 한 문장씩 설명할 수 있는가?
- 대표로 설명할 기능 3개를 고르고 각각 "문제·구현·검증·배운 점"을 말할 수 있는가?

### 검증 빈칸

- 전체 검증 명령과 통과 여부: ____
- api smoke test가 확인하는 흐름: ____
- 내가 직접 잡은 버그 1개와 원인: ____

### 설명 문장

Career Hub는 React로 ____를 관리하고, Express API로 ____와 ____ CRUD를 제공하며, bcrypt와 JWT로 보호 API를 구성한 제출용 fullstack 포트폴리오입니다.

## 09 TypeScript

관련 파일: `09-typescript/starter/*.ts`, `09-typescript/starter/*.tsx`

> 난이도 ★★☆ · 예상 시간 80분 · 선행 02, 03, 04

### 개념 빈칸

- TypeScript의 핵심 가치는 런타임 전에 ____ 시점에 오류를 잡는 것이다.
  - └ 힌트: 코드를 실행하기 전, JS로 바꾸는 그 시점. `npm run typecheck`가 도는 때.
- 객체의 구조를 이름 붙여 정의할 때 `____` 또는 `type`을 사용한다.
  - └ 힌트: `02-interface-design.ts` 파일 이름에 답이 들어 있다.
- 여러 가능한 타입을 표현하는 `string | number` 같은 타입을 ____ 타입이라고 한다.
  - └ 힌트: `|`로 여럿을 "합친" 타입.
- `typeof`, `instanceof`로 타입을 좁히는 과정을 ____이라고 한다.
  - └ 힌트: `05-narrowing.ts` 파일 이름 참고. 넓은 타입을 점점 "좁히는" 것.
- 호출자가 응답 타입을 지정하게 하는 문법 `fetchJson<T>`에서 T는 ____이다.
  - └ 힌트: `06-generic-fetch.ts`의 `<T>`. 타입을 매개변수처럼 받는 것.

### 구현 TODO

- [ ] 기본 타입, 배열, 객체 타입 빈칸을 채운다.
- [ ] Todo 컴포넌트 props 타입을 명시한다.
- [ ] Express Request params와 body 타입을 정의한다.
- [ ] `formatValue`에서 string, number, Date를 모두 처리한다.
  - └ 각 분기를 `typeof`/`instanceof`로 좁힌다.
  - └ 마지막 분기에서 `never`로 exhaustiveness를 확인한다.
- [ ] `fetchJson<T>`가 `Promise<T>`를 반환하도록 만든다.

### 흔한 실수·함정

- `any`를 남발해 타입 검사를 무력화한다(컴파일은 되지만 안전성은 0).
- 타입 단언(`as`)으로 에러를 "끄기"만 하고 실제 모양을 안 맞춘다.
- API 응답을 타입 없이 받아 프론트에서 런타임 에러가 난다.
- 컴파일 에러 메시지를 안 읽고 빨간 줄만 지운다.

### 면접 꼬리질문

- "TypeScript를 쓰면 뭐가 좋은가요? 단점은요?"
- "`interface`와 `type`의 차이는?"
- "`any`와 `unknown`의 차이는?"
- "제네릭은 왜 쓰나요? 예시를 하나 들어보세요."

### 자가 점검

- 같은 코드를 JS로 짰을 때 났을 버그를 TS가 컴파일 시점에 잡은 사례를 하나 말할 수 있는가?
- `never`로 exhaustiveness를 검사하면 새 분기가 생겼을 때 무슨 일이 일어나는가?

### 검증 빈칸

- 타입 검사 명령: ____
- `any`를 남기면 위험한 이유: ____
- 컴파일 시점에 잡은 오류 예시: ____

### 설명 문장

TypeScript 단계에서는 값의 모양을 ____로 명시해 API와 화면 사이의 계약이 어긋나는 문제를 미리 잡았습니다.

## 10 SQL/Oracle

관련 파일: `10-sql-oracle/starter/*.sql`

> 난이도 ★★★ · 예상 시간 2시간 · 선행 없음(독립) · **SI 면접 빈출**

SI 면접에서 SQL은 거의 항상 물어봅니다. 쿼리를 "읽고 쓰는" 것뿐 아니라 JOIN·트랜잭션·인덱스를 *말로* 설명할 수 있어야 합니다.

### 개념 빈칸

- 테이블을 만들고 변경하는 SQL 범주는 ____이다.
  - └ 힌트: CREATE/ALTER/DROP을 묶는 범주(Data ___ Language). `01-ddl.sql` 파일명 참고.
- 데이터를 조회, 삽입, 수정, 삭제하는 SQL 범주는 ____이다.
  - └ 힌트: SELECT/INSERT/UPDATE/DELETE를 묶는 범주(Data ___ Language).
- 기본키는 각 행을 ____하게 식별한다.
  - └ 힌트: 중복이 없고 비어 있지 않은 성질.
- 외래키는 두 테이블 사이의 ____ 관계를 보장한다.
  - └ 힌트: 한쪽에 없는 값을 다른 쪽이 참조하지 못하게 하는 무결성(참조 ___).
- `INNER JOIN`은 양쪽 조건이 맞는 행만 반환하고, `LEFT JOIN`은 ____ 테이블의 행을 모두 유지한다.
  - └ 힌트: JOIN 키워드 기준 어느 쪽을 다 남기는지 단어 그대로.
- 트랜잭션의 ACID 중 Atomicity는 ____를 의미한다.
  - └ 힌트: "전부 되거나 전부 안 되거나". 부분 성공을 허용하지 않는 성질.
- 인덱스는 조회 속도를 높일 수 있지만 ____ 비용과 저장 공간을 늘릴 수 있다.
  - └ 힌트: INSERT/UPDATE/DELETE 때 인덱스도 같이 고쳐야 한다. 그 작업 범주.

### 구현 TODO

- [ ] departments와 employees 테이블의 PK/FK를 정의한다.
- [ ] 더미 부서와 직원을 INSERT한다.
- [ ] WHERE와 ORDER BY로 조건 조회를 작성한다.
- [ ] GROUP BY와 HAVING으로 부서별 집계를 작성한다.
- [ ] INNER JOIN과 LEFT JOIN 결과 행 수를 비교한다.
- [ ] 서브쿼리로 평균보다 높은 급여를 조회한다.
- [ ] COMMIT과 ROLLBACK 결과를 직접 확인한다.
- [ ] 인덱스 생성 전후 실행 계획을 비교한다.

### 흔한 실수·함정

- `WHERE`로 거를 조건과 `HAVING`(집계 후 거르기)을 헷갈린다.
- `INNER JOIN`을 써놓고 "왜 한쪽에만 있는 행이 사라졌나" 당황한다(LEFT JOIN이 필요한 경우).
- `GROUP BY`에 없는 컬럼을 SELECT해 에러를 낸다.
- 인덱스를 "무조건 좋은 것"으로 외운다(쓰기 비용·선택도를 모름).
- 트랜잭션 없이 여러 줄을 바꾸다 중간 실패로 데이터가 깨진다.

### 면접 꼬리질문

- "INNER JOIN과 LEFT JOIN의 차이를 결과 행 수로 설명해보세요."
- "WHERE와 HAVING은 언제 각각 쓰나요?"
- "인덱스는 왜 조회를 빠르게 하나요? 언제 안 거나요?"
- "트랜잭션 ACID를 각각 한 문장으로 말해보세요."
- "이 쿼리의 실행 계획을 어떻게 봤나요?" (08/05의 데이터 흐름과 연결)
- "N+1 문제가 뭔지 아나요?"

### 자가 점검

- 같은 데이터로 INNER vs LEFT JOIN을 돌려 행 수 차이를 직접 봤는가?
- COMMIT 전후, ROLLBACK 후 데이터 상태를 직접 확인했는가?
- 인덱스 생성 전후 실행 계획(비용/스캔 방식)이 어떻게 달라졌는지 말할 수 있는가?

### 검증 빈칸

- 실행한 SQL 환경: ____
- INNER JOIN 결과 행 수: ____
- LEFT JOIN 결과 행 수: ____
- 인덱스 전후 실행 계획 차이: ____

### 설명 문장

SQL 단계에서는 테이블 관계를 ____로 보장하고, JOIN과 GROUP BY로 화면에 필요한 데이터를 조회하는 연습을 했습니다.

## 11 Java/Spring

관련 파일: `11-java-spring/README.md`, `11-java-spring/starter/README.md`

> 난이도 ★★★ · 예상 시간 2~3시간 · 선행 04, 09 · **SI 채용 핵심**

이 트랙은 "Node/Express에서 한 일을 Spring 구조로 옮겨 말하는" 다리입니다. 면접에서는 계층 분리와 DI를 자주 묻습니다.

### 개념 빈칸

- Spring에서 HTTP 요청을 받는 계층은 ____이다.
  - └ 힌트: Express의 라우터에 해당. `@RestController` 애너테이션이 붙는 곳.
- 비즈니스 규칙을 담당하는 계층은 ____이다.
  - └ 힌트: 컨트롤러와 저장소 사이. `@Service`가 붙는 곳.
- DB 접근을 담당하는 계층은 ____이다.
  - └ 힌트: 08단계 data-store에 해당. `@Repository`가 붙는 곳.
- 객체 생성을 프레임워크에 맡기고 필요한 곳에 주입받는 방식을 ____라고 한다.
  - └ 힌트: 생성자로 의존성을 받는 그 방식(Dependency ___). Spring의 핵심.
- API 요청/응답 전용 객체는 보통 ____라고 부른다.
  - └ 힌트: 도메인 엔티티와 분리해 전송에 쓰는 객체(Data Transfer ___).

### 구현 TODO

- [ ] 게시글 도메인 모델을 정의한다.
- [ ] Controller에서 REST 엔드포인트를 나눈다.
- [ ] Service에서 생성, 조회, 수정, 삭제 규칙을 처리한다.
- [ ] Repository에서 데이터 접근을 분리한다.
- [ ] DTO로 요청/응답 모양을 분리한다.
- [ ] 없는 게시글은 404로 응답한다.

### 흔한 실수·함정

- Controller에 비즈니스 로직을 다 넣어 계층 분리가 무너진다.
- 엔티티를 그대로 응답에 노출(DTO 미사용)해 내부 구조·민감 필드가 샌다.
- DI를 필드 주입(`@Autowired` 필드)으로만 알고 생성자 주입의 장점을 모른다.
- 예외를 일일이 try-catch로 처리(전역 예외 처리 `@ControllerAdvice`를 모름).

### 면접 꼬리질문

- "Controller-Service-Repository를 왜 나누나요? 안 나누면?"
- "DI(의존성 주입)가 뭔가요? 왜 좋나요? 생성자 주입을 왜 권장하나요?"
- "Express(08단계)와 Spring의 구조를 비교해보세요."
- "DTO와 Entity를 왜 분리하나요?"
- "`@Component`, `@Service`, `@Repository`의 차이가 있나요?"
- "JPA가 뭔가요? MyBatis와의 차이는?"

### 자가 점검

- 08단계 Express 코드의 라우터/검증/저장소를 Spring의 Controller/Service/Repository에 1:1로 대응시켜 말할 수 있는가?
- "객체를 내가 `new` 하지 않고 스프링이 만들어 준다"는 말의 의미와 이점을 설명할 수 있는가?

### 검증 빈칸

- 실행 명령: ____
- 게시글 목록 엔드포인트: ____
- Express와 Spring의 가장 큰 구조 차이: ____

### 설명 문장

Spring 단계에서는 Express에서 한 파일에 작성하던 API를 ____, ____, ____ 계층으로 나누어 유지보수하기 쉽게 만들었습니다.

## 12 Testing

관련 파일: `12-testing/starter/js/src/*.ts`

> 난이도 ★★☆ · 예상 시간 80분 · 선행 04, 09 · **SI 면접 자주 물음**

### 개념 빈칸

- 단일 함수의 작은 동작을 확인하는 테스트는 ____ 테스트다.
  - └ 힌트: `calculator.test.ts`가 그 예. 가장 작은 단위.
- 여러 모듈이 함께 동작하는 흐름을 확인하는 테스트는 ____ 테스트다.
  - └ 힌트: `app.test.ts`처럼 Express 앱 전체에 요청을 보내 확인하는 것.
- 외부 의존성을 가짜로 바꾸는 것을 ____이라고 한다.
  - └ 힌트: `notification.test.ts`에서 mailer를 가짜로 바꾸는 것(목/스텁 계열).
- 테스트의 기본 흐름 AAA는 Arrange, Act, ____이다.
  - └ 힌트: 준비 → 실행 → 그다음 "결과를 단언". A로 시작.
- Express API 테스트에서 HTTP 요청을 흉내내는 라이브러리는 ____이다.
  - └ 힌트: `app.test.ts`의 import를 보세요. `request(app).get(...)` 형태.

### 구현 TODO

- [ ] calculator 테스트에서 add, subtract, divide 정상 케이스를 검증한다.
- [ ] divide 0 나누기 예외를 검증한다.
- [ ] Express 앱 통합 테스트를 작성한다.
  - └ GET /posts 빈 배열을 확인한다.
  - └ POST /posts 생성 성공 상태 코드를 확인한다.
  - └ body 누락 시 400을 확인한다.
  - └ 없는 id 조회 시 404를 확인한다.
- [ ] mailer를 `vi.spyOn`으로 mock 처리한다.
- [ ] mock이 올바른 인자로 호출됐는지 검증한다.

### 흔한 실수·함정

- 정상 케이스만 테스트하고 예외·경계(0 나누기, 빈 입력, 없는 id)를 빼먹는다.
- 테스트가 실행 순서에 의존한다(이전 테스트가 남긴 상태에 기댐).
- mock을 안 풀어(`restore`) 다른 테스트에 영향을 준다.
- 단언 없이 "에러만 안 나면 통과"인 테스트를 짠다.

### 면접 꼬리질문

- "단위 테스트와 통합 테스트의 차이는?"
- "mock은 왜 쓰나요? 진짜 메일을 보내면 안 되나요?"
- "테스트를 짜면 뭐가 좋은가요? 커버리지 100%가 목표인가요?"
- "TDD를 들어봤나요? 어떻게 진행되나요?"
- "08단계 프로젝트는 어떤 테스트를 어떤 층위로 짰나요?"

### 자가 점검

- 내가 추가한 "회귀 방지" 케이스가 실제로 어떤 버그를 막는지 말할 수 있는가?
- 일부러 코드를 깨뜨렸을 때 테스트가 빨갛게 실패하는 걸 확인했는가?

### 검증 빈칸

- 테스트 실행 명령: ____
- 내가 추가한 회귀 방지 케이스: ____
- mock을 쓴 이유: ____

### 설명 문장

테스트 단계에서는 함수, API, 외부 의존성 호출을 자동 검증해 리팩터링 후 ____가 생겼는지 빠르게 확인할 수 있게 했습니다.

## 13 Git 협업

관련 파일: `13-git-collab/README.md`, `13-git-collab/problems.md`

> 난이도 ★★☆ · 예상 시간 60분 · 선행 없음(전 트랙과 병행)

### 개념 빈칸

- 기능 단위 작업 브랜치는 보통 ____ 브랜치라고 부른다.
  - └ 힌트: `feature/login`처럼 기능 이름을 붙이는 브랜치.
- GitHub에서 코드 리뷰와 병합을 요청하는 단위는 ____이다.
  - └ 힌트: PR. 풀어 쓰면?
- 두 브랜치가 같은 줄을 다르게 고치면 ____이 발생한다.
  - └ 힌트: merge 때 `<<<<<<<` 마커가 뜨는 그 상황.
- main 브랜치에 직접 push를 막는 설정은 ____ branch 규칙이다.
  - └ 힌트: GitHub의 "보호된 브랜치" 설정. 영어 단어 그대로.
- 이전 커밋을 정리해 직선 히스토리로 만드는 명령 흐름은 ____이다.
  - └ 힌트: merge 커밋 없이 커밋을 다시 얹는 명령.

### 구현 TODO

- [ ] feature 브랜치를 만든다.
- [ ] 작은 단위로 커밋 메시지를 작성한다.
- [ ] GitHub에서 PR을 연다.
- [ ] 리뷰 코멘트를 반영한다.
- [ ] 충돌을 재현하고 해결 기록을 남긴다.
- [ ] merge 후 로컬 main을 최신화한다.

### 흔한 실수·함정

- main에 직접 커밋·푸시한다.
- 커밋 하나에 관련 없는 변경을 몰아넣어 리뷰가 어렵다.
- 충돌 마커(`<<<<<<<`, `=======`, `>>>>>>>`)를 그대로 커밋한다.
- `git push -f`로 남의 작업을 날린다.
- 커밋 메시지가 "수정", "ㅇㅇ"처럼 무의미하다.

### 면접 꼬리질문

- "merge와 rebase의 차이는?"
- "충돌이 나면 어떻게 해결하나요?"
- "커밋 메시지는 어떤 규칙으로 쓰나요?"
- "협업할 때 브랜치 전략을 어떻게 가져갔나요?"
- "PR 리뷰에서 어떤 점을 보나요?"

### 자가 점검

- 일부러 충돌을 만들고 해결한 경험이 있는가? 그 과정을 말할 수 있는가?
- `git log --oneline`으로 본 내 히스토리가 남에게 읽히는 모양인가?

### 검증 빈칸

- 브랜치 생성 명령: ____
- PR 제목: ____
- 충돌 해결한 파일: ____

### 설명 문장

Git 협업 단계에서는 기능 브랜치와 PR을 통해 변경을 검토하고, 충돌이 났을 때 ____를 기준으로 안전하게 해결했습니다.

## 14 Docker와 CI

관련 파일: `14-docker-deploy/starter/**`, `.github/workflows/ci.yml`

> 난이도 ★★★ · 예상 시간 2시간 · 선행 07, 13

### 개념 빈칸

- Docker 이미지를 만드는 설계 파일은 ____이다.
  - └ 힌트: 대문자 D로 시작, 확장자 없는 파일.
- 컨테이너가 실행할 기본 명령은 `CMD` 또는 ____로 정의한다.
  - └ 힌트: CMD와 비슷하지만 인자 덮어쓰기를 막는 명령어. 대문자.
- 빌드 캐시를 살리려면 `package*.json`을 먼저 복사한 뒤 ____를 실행한다.
  - └ 힌트: 소스 전체 복사 전에 의존성부터 설치하는 그 명령(`npm ___`).
- 여러 컨테이너를 함께 실행하는 도구는 docker ____이다.
  - └ 힌트: `docker-compose.yml`로 DB+앱을 같이 띄우는 그것.
- GitHub Actions에서 push/PR마다 자동 실행되는 파이프라인을 ____라고 한다.
  - └ 힌트: 지속적 통합(Continuous ___). 약자 두 글자.
- 이미지에 포함하면 안 되는 로컬 파일 목록은 ____에 둔다.
  - └ 힌트: `.gitignore`의 도커판. 점으로 시작.

### 구현 TODO

- [ ] Node 앱 Dockerfile을 완성한다.
  - └ base image를 채운다.
  - └ package 파일 먼저 복사 후 `npm ci`를 실행한다.
  - └ 소스 전체를 복사하고 포트를 노출한다.
- [ ] Spring Boot Dockerfile을 build/runtime 멀티 스테이지로 나눈다.
- [ ] compose에서 Postgres 이미지와 볼륨을 설정한다.
- [ ] GitHub Actions에서 checkout, setup-node, install, test, build 단계를 구성한다.
- [ ] 시크릿이 Dockerfile이나 커밋에 들어가지 않았는지 확인한다.

### 흔한 실수·함정

- `COPY . .`을 맨 앞에 둬 캐시가 매번 깨진다(의존성 설치가 매번 다시 됨).
- `.dockerignore`를 안 만들어 `node_modules`, `.env`가 이미지에 들어간다.
- 멀티 스테이지를 안 써서 최종 이미지에 빌드 도구·소스가 다 남아 비대해진다.
- 시크릿을 Dockerfile `ENV`에 박는다.

### 면접 꼬리질문

- "Docker를 왜 쓰나요? '제 컴퓨터에선 됐는데'를 어떻게 줄이나요?"
- "이미지와 컨테이너의 차이는?"
- "멀티 스테이지 빌드는 왜 쓰나요?"
- "CI에서 뭘 자동으로 돌리나요? 왜 그걸 자동화하나요?"
- "레이어 캐시를 살리려면 Dockerfile 순서를 어떻게 짜나요?"

### 자가 점검

- 빌드 → 실행 → compose까지 직접 명령으로 돌려봤는가?
- CI가 빨갛게 실패한 걸 보고 로그를 읽어 고친 적이 있는가?

### 검증 빈칸

- Docker 빌드 명령: ____
- 컨테이너 실행 명령: ____
- compose 실행 명령: ____
- CI에서 자동 확인할 명령: ____

### 설명 문장

Docker 단계에서는 앱과 실행 환경을 ____로 묶고, CI에서 빌드와 테스트를 자동화해 "내 컴퓨터에서는 됨" 문제를 줄였습니다.

## 15 CS Fundamentals

관련 파일: `15-cs-fundamentals/problems.md`

> 난이도 ★★☆ · 예상 시간 90분 · 선행 04, 06, 10 (구현과 연결해 공부)

CS는 암기가 아니라 "내 코드와 연결해 설명"하는 게 핵심입니다. 빈칸을 채운 뒤 반드시 자기 트랙 파일과 엮으세요.

### 개념 빈칸

- HTTP는 OSI 7계층 중 ____ 계층 프로토콜이다.
  - └ 힌트: 가장 위, 사용자 가까운 계층(7계층). 영어로.
- TCP는 신뢰성을 제공하고, UDP는 ____이 낮고 빠르다.
  - └ 힌트: 연결·재전송을 안 해서 줄어드는 그것(지연/부담 계열).
- 프로세스는 실행 중인 프로그램이고, 스레드는 프로세스 안의 ____ 단위다.
  - └ 힌트: 한 프로세스 안에서 메모리를 공유하며 도는 더 작은 "실행" 단위.
- DB 트랜잭션의 격리 수준이 낮으면 ____ read 같은 문제가 생길 수 있다.
  - └ 힌트: 커밋 안 된 데이터를 읽는 현상(dirty 계열). 10단계 트랜잭션과 연결.
- Big-O는 입력 크기가 커질 때 알고리즘의 ____ 변화를 표현한다.
  - └ 힌트: 시간(또는 공간) 비용이 어떻게 자라는지. "복잡도".

### 구현 연결 TODO

- [ ] HTTP 상태 코드를 04, 08 단계 API와 연결해 설명한다.
- [ ] TCP/UDP를 브라우저와 API 통신 흐름에 연결한다.
- [ ] 프로세스/스레드를 Node 서버 실행과 연결한다.
- [ ] ACID를 10단계 트랜잭션과 연결한다.
- [ ] 자료구조를 Todo 배열, 게시글 목록, Map/Set 예시와 연결한다.

### 흔한 실수·함정

- 정의를 외우기만 하고 "내 코드 어디에 쓰였는지"를 못 댄다 → 꼬리질문에서 무너진다.
- TCP/UDP, 프로세스/스레드를 한 문장으로만 외워 차이를 못 든다.
- Big-O를 공식처럼 외우고 실제 코드의 반복문과 못 잇는다.

### 면접 꼬리질문

- "브라우저 주소창에 URL을 치고 Enter를 누르면 무슨 일이 일어나나요?" (단골 종합 질문)
- "프로세스와 스레드의 차이는? Node는 싱글 스레드인데 어떻게 동시 요청을 처리하나요?"
- "HTTP와 HTTPS의 차이는?"
- "내가 만든 기능 중 자료구조 선택이 성능에 영향을 준 곳이 있나요?"

### 자가 점검

- 각 CS 개념을 "내 트랙 파일 ___에서 이렇게 쓰였다"로 한 문장씩 댈 수 있는가?
- 가장 약한 주제 1개를 알고, 1분 답변을 녹음해 들어봤는가?

### 검증 빈칸

- 가장 약한 CS 주제: ____
- 내 코드와 연결한 파일: ____
- 1분 답변 녹음 여부: ____

### 설명 문장

CS 단계에서는 암기 답안이 아니라, 네트워크와 DB, 자료구조 개념을 내가 만든 ____ 기능과 연결해 설명하는 연습을 했습니다.

## 16 Security

관련 파일: `16-security/starter/**`

> 난이도 ★★★ · 예상 시간 2시간 · 선행 06, 07 · **공격 재현 → 방어 순서로**

### 개념 빈칸

- 저장형 XSS는 악성 스크립트가 ____에 저장된 뒤 다른 사용자에게 실행되는 공격이다.
  - └ 힌트: 게시글처럼 서버 DB/저장소에 "남는" 곳. `01-xss-stored.js` 참고.
- 반사형 XSS는 요청 파라미터가 응답 HTML에 그대로 ____될 때 발생한다.
  - └ 힌트: 검색어가 그대로 화면에 "되비쳐" 박히는 것(반사/반영 계열).
- XSS 방어의 핵심은 입력을 무조건 믿지 않고 출력 시점에 HTML ____를 적용하는 것이다.
  - └ 힌트: `<`를 `&lt;`로 바꾸는 그 변환(escape/인코딩 계열). `escapeHtml` 함수.
- SQL Injection 방어는 문자열 연결 대신 ____ 쿼리를 사용한다.
  - └ 힌트: `?` placeholder에 값을 따로 바인딩하는 방식(파라미터화/준비된). `03-sql-injection.js`.
- CSRF는 사용자가 로그인된 상태를 악용해 원치 않는 ____을 보내게 한다.
  - └ 힌트: 사용자가 의도하지 않은 "요청". `04-csrf-demo` 참고.
- CORS에서 credential 요청을 허용할 때 `origin: "*"`와 함께 쓰면 ____하다.
  - └ 힌트: 아무 출처나 쿠키를 들고 호출할 수 있게 되니 보안상…(위험/취약 계열). `05-cors.js`.

### 구현 TODO

- [ ] `escapeHtml`에서 `&`, `<`, `>`, `"`, `'`를 안전한 엔티티로 바꾼다.
- [ ] 저장형 XSS 출력 지점에 escape를 적용한다.
- [ ] 반사형 XSS 검색어와 input value에 escape를 적용한다.
- [ ] SQL 검색을 `?` placeholder와 값 배열로 바꾼다.
- [ ] CSRF 방어를 적용한다.
  - └ SameSite 쿠키를 적용한다.
  - └ CSRF 토큰을 hidden input으로 전달한다.
- [ ] CORS를 안전하게 설정한다.
  - └ 허용 출처 whitelist를 적용한다.
  - └ credential 허용 여부를 명시한다.

### 흔한 실수·함정

- 입력 시점에만 막고 출력 인코딩을 빼먹는다(방어의 핵심은 출력 시점).
- escape를 일부 출력 지점에만 적용해 한 군데가 뚫린다.
- SQL을 문자열 연결로 만들고 "내 입력은 안전하다"고 가정한다.
- CORS를 `origin: "*"` + `credentials: true`로 같이 켠다(브라우저가 막거나, 막아야 할 걸 연다).

### 면접 꼬리질문

- "XSS가 뭔가요? 저장형과 반사형의 차이는? 어떻게 막나요?"
- "SQL Injection을 어떻게 막나요? 왜 문자열 연결이 위험한가요?"
- "CSRF가 뭔가요? 토큰과 SameSite 쿠키는 각각 어떻게 막나요?"
- "06단계에서 JWT를 localStorage에 뒀다면 XSS에 어떤 위험이 있나요?" (트랙 간 연결)
- "8단계에서 helmet/rate limit은 무슨 공격을 줄이나요?"

### 자가 점검

- 각 공격을 *먼저 재현*해서 뚫리는 걸 눈으로 본 뒤 방어했는가?
- 방어 전후 응답/화면이 어떻게 달라졌는지 말할 수 있는가?
- 6단계 인증, 8단계 보안 미들웨어와 연결해 설명할 수 있는가?

### 검증 빈칸

- 재현한 공격 payload: ____
- 방어 후 화면/응답 변화: ____
- 6단계 인증과 연결되는 보안 개념: ____

### 설명 문장

보안 단계에서는 공격을 먼저 재현한 뒤, 출력 인코딩, 파라미터화 쿼리, CSRF 토큰, CORS whitelist로 ____를 줄였습니다.

## 17 Interview Prep

관련 파일: `17-interview-prep/interview-cards.md`, `17-interview-prep/self-intro-templates.md`, `17-interview-prep/behavioral-questions.md`, `17-interview-prep/project-pitch-template.md`

> 난이도 ★★☆ · 예상 시간 진행형(계속 다듬음) · 선행 01~16 전체

### 개념 빈칸

- 좋은 기술 면접 답변은 "무엇을 했다"보다 "왜 그렇게 했고 어떻게 ____했다"를 포함한다.
  - └ 힌트: 동작을 어떻게 "확인"했는지. 12단계 키워드와 통한다(검증).
- STAR 답변은 Situation, Task, Action, ____ 순서로 구성한다.
  - └ 힌트: 마지막은 "그래서 어떤 결과가 났는가". R로 시작.
- 프로젝트 설명의 첫 문장은 해결한 ____를 말해야 한다.
  - └ 힌트: 기술 나열 전에 "무슨 불편/문제를 풀었나".
- 꼬리질문 대비에는 사용한 기술의 장점뿐 아니라 ____도 준비해야 한다.
  - └ 힌트: "그 기술의 단점/트레이드오프는?" 질문 대비.

### 작성 TODO

- [ ] 30초 자기소개를 작성한다.
- [ ] 1분 자기소개를 작성한다.
- [ ] 3분 자기소개를 작성한다.
- [ ] Career Hub 기술 설명 카드를 작성한다.
- [ ] 가장 어려웠던 오류와 해결 과정을 STAR로 작성한다.
- [ ] 협업/갈등/학습 경험 답변을 작성한다.
- [ ] 프로젝트 피치에서 문제, 기능, 기술, 검증, 배운 점을 모두 포함한다.

### 흔한 실수·함정

- 기술 나열만 하고 "왜·결과"가 없다.
- 외운 티가 나는 자기소개(말이 끊기면 무너짐).
- 단점을 못 말해 "깊이가 얕다"는 인상을 준다.
- 답변이 길어 시간 초과(30초/1분/3분 버전을 안 나눔).
- 거짓·과장(안 해본 걸 했다고 함) → 꼬리질문에서 들통난다.

### 면접 꼬리질문 (대비 세트)

- "본인을 한 문장으로 소개해보세요."
- "가장 자신 있는 프로젝트 하나를 설명해보세요."
- "그 프로젝트에서 가장 어려웠던 점과 해결 방법은?"
- "왜 그 기술을 골랐나요? 다른 선택지는 없었나요?"
- "협업 중 갈등을 어떻게 풀었나요?"
- "최근에 새로 공부한 것은?"

### 자가 점검

- 30초/1분/3분 자기소개를 스톱워치로 맞춰 말해봤는가?
- 대표 기능 3개에 대해 꼬리질문 2개씩을 스스로 던지고 답해봤는가?
- 모든 답이 *내가 만든 코드·검증 근거*에 닿아 있는가? (거짓·과장 0)

### 검증 빈칸

- 가장 자신 있는 기술 질문: ____
- 가장 약한 꼬리질문: ____
- 스톱워치로 맞춘 답변 시간: ____

### 설명 문장

면접 준비 단계에서는 1~16단계의 구현 근거를 모아, 내가 직접 만든 ____를 기준으로 기술과 협업 경험을 설명합니다.

## monorepo-mini-app Note Hub

관련 파일: `monorepo-mini-app/packages/shared/src/index.ts`, `monorepo-mini-app/packages/api/src/server.ts`, `monorepo-mini-app/packages/api/src/notes-store.ts`, `monorepo-mini-app/packages/api/src/notes-store-pg.ts`, `monorepo-mini-app/packages/web/src/App.tsx`

> 난이도 ★★★ · 예상 시간 2~3시간 · 선행 09, 12, 14 · **타입 공유·계약 검증 데모**

### 개념 빈칸

- 여러 패키지를 한 저장소에서 관리하는 구조를 ____라고 한다.
  - └ 힌트: 폴더 이름 그대로(mono…). 하나의 repo에 여러 패키지.
- npm 기본 기능으로 패키지들을 묶는 설정은 ____이다.
  - └ 힌트: 루트 `package.json`의 `"workspaces"` 필드. npm ___.
- API와 Web이 함께 import하는 공통 패키지는 `@note-hub/____`이다.
  - └ 힌트: 타입을 공유하는 패키지 폴더 이름.
- 백엔드 응답 타입이 바뀌면 프론트엔드 ____가 실패해 계약 불일치를 빨리 발견한다.
  - └ 힌트: 09단계의 그 시점 검사(타입 검사/컴파일).
- `NotesStore` 인터페이스 뒤에 InMemory와 PostgreSQL 구현을 둔 이유는 저장 방식 ____를 쉽게 하기 위해서다.
  - └ 힌트: 08단계 저장소 계층 분리와 같은 동기(교체/대체 계열).
- `DATABASE_URL`이 있으면 ____ 저장소를 사용하고, 없으면 ____ 저장소를 사용한다.
  - └ 힌트: 환경변수가 있으면 진짜 DB(Postgres), 없으면 메모리. `server.ts`의 분기를 보세요.

### 구현 TODO

- [ ] shared 패키지에 `Note`, `CreateNoteInput`, `ApiRoutes`를 정의한다.
- [ ] API 서버에서 `ApiRoutes.notes`를 사용해 목록과 생성 라우트를 만든다.
- [ ] `validateCreate`로 title, body, tags 입력을 검증한다.
- [ ] InMemory store와 Postgres store가 같은 인터페이스를 만족하게 한다.
- [ ] Web에서 상태 타입을 고정한다.
  - └ `useState<Note[]>`로 목록 타입을 고정한다.
  - └ `useState<CreateNoteInput>`으로 폼 타입을 고정한다.
- [ ] Web에서 note 생성 후 목록을 다시 불러온다.
- [ ] OpenAPI 스펙과 shared 타입의 동기화 테스트를 유지한다.
- [ ] Dockerfile에서 web 빌드 결과와 API 런타임을 단일 이미지에 담는다.

### 흔한 실수·함정

- shared 패키지를 빌드 안 하고 API/Web에서 import해 `dist` 없음 에러가 난다.
- 두 store 구현의 시그니처가 어긋나 인터페이스를 안 만족한다.
- OpenAPI 스펙을 바꾸고 타입을 안 맞춰 동기화 테스트가 깨진다.
- 프론트 상태에 `any`를 써서 모노레포의 타입 공유 장점을 버린다.

### 면접 꼬리질문

- "모노레포를 왜 쓰나요? 멀티레포 대비 장단점은?"
- "타입을 양쪽이 공유하면 어떤 버그를 컴파일 시점에 막나요?"
- "`NotesStore` 인터페이스 뒤에 두 구현을 둔 이유는? (08단계와 무엇이 같나요?)"
- "OpenAPI 스펙과 타입을 어떻게 동기화하나요? 테스트로 강제했나요?"

### 자가 점검

- 백엔드 응답 타입을 일부러 바꿨을 때 프론트 타입 검사가 실패하는 걸 확인했는가?
- `DATABASE_URL` 유무로 저장소가 바뀌는 걸 직접 실행해봤는가?

### 검증 빈칸

- shared 빌드 명령: ____
- 전체 테스트 명령: ____
- API 포트: ____
- Web 포트: ____
- Postgres 실행 명령: ____

### 설명 문장

Note Hub는 npm workspaces로 API, Web, shared를 나누고, ____ 타입을 양쪽이 함께 사용해 API 계약을 컴파일 시점에 검증하는 모노레포 데모입니다.

## 전체 기능 완성 체크 매트릭스

각 줄마다 네 칸이 모두 채워지면 해당 단계는 "공부용으로 설명 가능한 상태"입니다. 마지막 두 칸(꼬리질문·약점)은 면접 대비 상태를 추적합니다.

- 개념 설명: 빈칸을 자기 말로 채웠다.
- 구현 완료: TODO를 실제 코드/문서에서 끝냈다.
- 검증 완료: 브라우저·API·테스트·빌드 중 하나로 확인했다.
- 면접 문장: 설명 문장을 1분 안에 말할 수 있다.
- 꼬리질문: 그 단계 꼬리질문에 막힘없이 답했다.
- 약점 메모: 아직 약한 항목을 적어 뒀다(없으면 `-`).

| 단계 | 난이도 | 개념 | 구현 | 검증 | 면접 문장 | 꼬리질문 | 약점 메모 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 01 HTML/CSS | ★☆☆ | [ ] | [ ] | [ ] | [ ] | [ ] |  |
| 02 JavaScript | ★★☆ | [ ] | [ ] | [ ] | [ ] | [ ] |  |
| 03 React Todo | ★★☆ | [ ] | [ ] | [ ] | [ ] | [ ] |  |
| 04 Node API | ★★☆ | [ ] | [ ] | [ ] | [ ] | [ ] |  |
| 05 MongoDB | ★★☆ | [ ] | [ ] | [ ] | [ ] | [ ] |  |
| 06 Auth | ★★★ | [ ] | [ ] | [ ] | [ ] | [ ] |  |
| 07 Deploy | ★★☆ | [ ] | [ ] | [ ] | [ ] | [ ] |  |
| 08 Career Hub | ★★★ | [ ] | [ ] | [ ] | [ ] | [ ] |  |
| 09 TypeScript | ★★☆ | [ ] | [ ] | [ ] | [ ] | [ ] |  |
| 10 SQL | ★★★ | [ ] | [ ] | [ ] | [ ] | [ ] |  |
| 11 Spring | ★★★ | [ ] | [ ] | [ ] | [ ] | [ ] |  |
| 12 Testing | ★★☆ | [ ] | [ ] | [ ] | [ ] | [ ] |  |
| 13 Git | ★★☆ | [ ] | [ ] | [ ] | [ ] | [ ] |  |
| 14 Docker/CI | ★★★ | [ ] | [ ] | [ ] | [ ] | [ ] |  |
| 15 CS | ★★☆ | [ ] | [ ] | [ ] | [ ] | [ ] |  |
| 16 Security | ★★★ | [ ] | [ ] | [ ] | [ ] | [ ] |  |
| 17 Interview | ★★☆ | [ ] | [ ] | [ ] | [ ] | [ ] |  |
| Note Hub | ★★★ | [ ] | [ ] | [ ] | [ ] | [ ] |  |

## 우선순위 가이드 (SI/Java-Spring 채용 기준)

시간이 부족할 때 이 순서로 채우면 면접 대비 효율이 높습니다.

1. **08 Career Hub** — 대표 프로젝트. 가장 길게 말하게 된다.
2. **10 SQL** — SI 면접 거의 필수.
3. **11 Spring** — Java/Spring 채용의 핵심. 08을 Spring 구조로 옮겨 말하기.
4. **06 Auth + 16 Security** — 인증·보안은 단골 꼬리질문.
5. **12 Testing** — "테스트 짜봤나요"에 코드로 답하기.
6. **09 TypeScript / monorepo** — 타입 안전·계약 검증 강점.
7. 나머지(01~05, 07, 13~15, 17)로 기초와 설명을 보강.

## 최종 제출 전 TODO

- [ ] 루트에서 `npm run verify`를 실행했다.
- [ ] `08-fullstack-portfolio-project`에서 `npm run verify`를 실행했다.
- [ ] `monorepo-mini-app`에서 `npm test` 또는 필요한 범위의 테스트를 실행했다.
- [ ] `npm run progress`로 남은 빈칸이 0인지 확인했다.
- [ ] `.env`, `node_modules`, `dist`, 로컬 데이터 파일이 커밋되지 않는지 확인했다.
- [ ] README에 실행 방법, 주요 기능, 검증 명령이 들어 있는지 확인했다.
- [ ] 면접에서 설명할 대표 기능 3개를 골랐다: ____, ____, ____.
- [ ] 각 대표 기능에 대해 "문제, 구현, 검증, 배운 점" 4문장을 작성했다.
- [ ] 각 대표 기능마다 예상 꼬리질문 2개와 답을 준비했다: ____, ____.
- [ ] 가장 약한 트랙 1개를 알고, 그 보강 계획을 적었다: ____.

## 최종 설명 템플릿

제가 만든 프로젝트는 ____ 문제를 해결하기 위한 ____ 앱입니다. 프론트엔드는 ____로 화면 상태와 폼 흐름을 관리했고, 백엔드는 ____로 REST API를 구성했습니다. 데이터는 ____에 저장하며, 인증은 ____와 ____로 구현했습니다. 마지막으로 ____ 명령으로 빌드, 테스트, 제출 감사를 확인했습니다.

### 꼬리질문 1분 답변 틀 (STAR)

- **상황(S):** 어떤 문제/요구가 있었나 — ____
- **과제(T):** 내가 맡은 부분 — ____
- **행동(A):** 어떤 파일에서 무엇을 어떻게 구현했나(근거 인용) — ____
- **결과(R):** 무엇으로 검증했고 무엇이 나아졌나 — ____
